#include "include.h"

// char mapTest[10][10];
// fopen(file.txt, rb");
// FILE* sp = fopen("file.txt", "r"); open readable file
// fseek(sp, 0, SEEK_END); end of file
// rewind(sp); beginning of file
// for (i=0,m feof(sp) == 0, i++)
// {
//	carac = fgetc(sp);
//	Data_in[i] = carac;
// }
// fclose(sp); close file