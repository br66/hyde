#include <stdio.h>

int main(int argc, char *argv[])
{
	bool done = false;

	printf("Project2017 SDL ver2");

	do
	{
		//pull(); // rename poll or input
		//update();
		//draw();
	} 
	while (!done);

	return 0;
}